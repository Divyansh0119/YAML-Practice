name: My first Workflow

on: [push]

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - run: ls

      - name: Checkout
        uses: actions/checkout@v4

      - name: Print Hello
        run: echo "Hello Divyansh"

      - name: Print date
        run: date
  
      - name: Execution of multiple commands
        run: |
          pwd
          ls
          whoami
          uname -a

      - run: ls